<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.FileMapper">
	<insert id="insertFile">
		insert into file values(#{sys_name},#{org_name},#{board_num})
	</insert>
	
	<select id="getFiles">
		select * from file where board_num=#{board_num}
	</select>
	
	<delete id="deleteBySystemname">
		delete from file where sys_name=#{sys_name}
	</delete>
</mapper>







