<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.ReplyMapper">
	<insert id="insertReply">
		insert into reply(reply_content, user_id, board_num)
		values(#{reply_content},#{user_id},#{board_num})
	</insert>
	
	<update id="updateReply">
		update reply set reply_content=#{reply_content}, update_date=now()
		where reply_num=#{reply_num}
	</update>
	
	<delete id="deleteReply">
		delete from reply where reply_num=#{reply_num}
	</delete>
	<delete id="deleteByBoardnum">
		delete from reply where board_num=#{board_num}
	</delete>
	
	<select id="getTotal">
		select count(*) from reply where boardnum=#{boardnum}
	</select>
	<select id="getRecentReply">
		<![CDATA[
			select count(*) from reply where board_num=#{board_num} and timestampdiff(HOUR,regdate,now())<1
		]]>
	</select>
	<select id="getLastNum">
		select max(reply_num) from reply where user_id=#{user_id}
	</select>
	<select id="getList">
		select * from reply where board_num=#{board_num}
		limit #{cri.startrow},#{cri.amount}
	</select>
</mapper>







