<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.CalorieMapper">
    <select id="findByFoodName" parameterType="string" resultType="com.kh.demo.dto.FoodDTO">
        SELECT * FROM food WHERE food_name LIKE CONCAT('%', #{keyword}, '%');
    </select>
</mapper>