<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.CalorieMapper">
    <select id="findByFoodName">
        SELECT * FROM food WHERE foodName LIKE CONCAT('%', #{keyword}, '%') LIMIT 30;
    </select>

    <select id="getTop30FoodsByViewCnt">
        SELECT * FROM food ORDER BY viewCnt DESC, food_num ASC LIMIT 30;
    </select>
    <select id="getView">
        select * from food where food_num=#{foodNum};
    </select>
    <update id="updateViewCount">
        update food set viewCnt = viewCnt+1 where food_num = #{foodNum}
    </update>
</mapper>