<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.BoardMapper">
	<sql id="cri">
		<if test="keyword != '' and type != ''">
			<trim prefixOverrides="or" prefix="(" suffix=") and">
				<foreach collection="typeArr" item="type">
					or 
					<choose>
						<when test="type == 'T'.toString()">
							(board_title like('%${keyword}%'))
						</when>
						<when test="type == 'C'.toString()">
							(board_content like('%${keyword}%'))
						</when>
						<when test="type == 'W'.toString()">
							(user_id like('%${keyword}%'))
						</when>
					</choose>
				</foreach>
			</trim>
		</if> 
	</sql>
	
	<insert id="insertBoard">
		insert into board (board_title,board_content,user_id)
		values(#{board_title},#{board_content},#{user_id})
	</insert>
	
	<update id="updateReadCount">
		update board set view_cnt = view_cnt+1 where board_num = #{board_num}
	</update>
	<update id="updateBoard">
		update board set board_title=#{board_title}, board_content=#{board_content},update_date=now()
		where board_num=#{board_num}
	</update>
	
	<delete id="deleteBoard">
		delete from board where board_num=#{board_num}
	</delete>
	
	<select id="getList">
		select * from board where
		<include refid="cri"></include>
		<![CDATA[ 
			0 < board_num order by board_num desc limit #{startrow},#{amount}
		]]>
	</select>
	<select id="getTotal">
		select count(*) from board where
		<include refid="cri"></include> board_num > 0
	</select>
	<select id="getLastNum">
		select max(board_num) from board where user_id=#{user_id}
	</select>
	<select id="findByNum">
		select * from board where board_num=#{board_num}
	</select>
</mapper>







