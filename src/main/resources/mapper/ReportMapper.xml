<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kh.demo.mapper.ReportMapper">
    <insert id="insertReport">
        insert into report (user_id, board_category, report_boardnum, report_content, reported_user)
        values (#{userId}, #{boardCategory}, #{boardNum}, #{reportContent}, #{reportedUserId})
    </insert>
</mapper>
